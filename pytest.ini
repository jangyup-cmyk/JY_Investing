[pytest]
asyncio_mode = auto
addopts = --strict-markers -m "not integration and not manual"
markers =
    integration: tests that connect to external services or use real credentials
    manual: scripts that are run manually and may place orders or trigger side effects
